import { GridColDef } from '../../models/colDef/index';
import { GridOptions } from '../../models/gridOptions';
import { GridSortDirection } from '../../models/gridSortModel';
interface GridColumnHeaderItemProps {
    colIndex: number;
    column: GridColDef;
    isDragging: boolean;
    isResizing: boolean;
    sortDirection: GridSortDirection;
    sortIndex?: number;
    options: GridOptions;
    filterItemsCounter?: number;
}
export declare const GridColumnHeaderItem: ({ column, colIndex, isDragging, isResizing, sortDirection, sortIndex, options, filterItemsCounter, }: GridColumnHeaderItemProps) => JSX.Element;
export {};
